import React from "react";

import CheckIcon from "@material-ui/icons/Check";

export default function AboutCheckmark() {
  const style = {
    fontSize: "3rem",
  };

  return <CheckIcon style={style} />;
}
